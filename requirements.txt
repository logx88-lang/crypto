# Pinned manifest (verified 2026-07-13). All have win_amd64 wheels (cp311/cp312).
# ASCII only: pip reads requirement files with the system locale (cp949 on Korean
# Windows) when there is no BOM, so non-ASCII comments break offline install.
# Offline install:
#   pip install --no-index --find-links wheelhouse/win_amd64_pyXY -r requirements.txt

# --- document parsers (deterministic core) ---
openpyxl==3.1.5
python-docx==1.2.0
pywin32==312          # .doc(구형 워드) → docx 변환용 Word COM (Windows, Word 설치 시). 미설치면 LibreOffice 폴백
python-pptx==1.0.2
pdfplumber==0.11.10
charset-normalizer==3.4.9

# --- indexing / retrieval ---
chromadb==1.5.9
kiwipiepy==0.23.2
kiwipiepy_model==0.23.0
bm25s==0.3.9
ollama==0.6.2

# --- reranker (CPU) ---
torch==2.13.0
sentence-transformers==5.6.0
transformers==5.13.1

# --- UI (web: rag/webui, port 8502) — Starlette + HTMX, pure-python wheels ---
# Static assets (htmx.min.js, tailwind.js) are bundled in the repo: no CDN/internet.
starlette==1.3.1
uvicorn==0.51.0
jinja2==3.1.6
python-multipart==0.0.32
anyio==4.14.2                    # starlette/uvicorn dep (pure wheel)
h11==0.16.0                     # uvicorn HTTP dep (pure wheel)
click==8.4.2                    # uvicorn CLI dep (pure wheel)
# --- UI (legacy Streamlit, port 8501) — fallback until web UI is settled ---
streamlit==1.59.1
streamlit-paste-button==0.1.2   # 개선기록 클립보드 이미지 붙여넣기(순수 wheel). 없으면 파일 업로드로 대체

# --- OCR (Phase 3: images / scanned PDF). Set RAG_OCR=0 to disable. ---
paddlepaddle==3.3.1
paddleocr==3.7.0
